Commit message

Seed the DB.

- Create a rake task to seed the DB with products, brands, and categories from Faker.
  - Create a pics folder in app's home directory to store downloaded images.
- Add Faker gem for seeding DB with random data.
- Add link to homepage on 'Brand' in navbar.
- Add some Bootstrap styles to products/show and wrap the application layout in a container.

