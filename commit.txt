Commit message

Add validations for Product, Quantity, and Category models.

Create and run migration to add an index on name in the Category
table and make the name unique.
Remove TODO from Category model.

